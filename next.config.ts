import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['youthful-manatee-762.convex.cloud', 'opulent-mammoth-378.convex.cloud'],
  },
};

export default nextConfig;
